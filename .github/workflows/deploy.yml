name: Deploy

on:
  push:
    branches: [main]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  secrets-check:
    runs-on: ubuntu-latest
    steps:
      - name: Validate required secrets
        run: |
          MISSING=""
          for secret in ORACLE_HOST ORACLE_USER ORACLE_SSH_KEY DOMAIN; do
            if [ -z "${{ secrets[secret] }}" ]; then
              MISSING="$MISSING $secret"
            fi
          done
          if [ -n "$MISSING" ]; then
            echo "::error::Missing required secrets:$MISSING"
            echo "::error::Add these secrets in GitHub repository Settings > Secrets and variables > Actions"
            exit 1
          fi
          echo "All required secrets are present"

  test:
    uses: ./.github/workflows/ci.yml
    needs: [secrets-check]

  deploy:
    needs: [test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Oracle Cloud VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.ORACLE_HOST }}
          username: ${{ secrets.ORACLE_USER }}
          key: ${{ secrets.ORACLE_SSH_KEY }}
          script: |
            set -e
            cd /app/retromind

            # Pull latest code
            git pull origin main

            # Copy env file from secure location
            cp /app/.env.prod .env.prod

            # Build and restart (ARM64 native on Ampere)
            docker compose -f docker-compose.prod.yml build --pull
            docker compose -f docker-compose.prod.yml up -d

            # Clean up old images
            docker system prune -af --filter "until=24h"

            # Verify services are up
            sleep 10
            docker compose -f docker-compose.prod.yml ps

  smoke-test:
    needs: [deploy]
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test frontend
        run: curl -sfI https://${{ secrets.DOMAIN }} 2>&1 | head -5
      - name: Smoke test API
        run: curl -sf https://api.${{ secrets.DOMAIN }}/api/v1/health 2>&1 | head -5
